rules_version = '2';

// Copia este contenido en la consola de Firebase → Firestore → Reglas.
// Sustituye cualquier regla `allow read, write: if false` que bloquee todo.

service cloud.firestore {
  match /databases/{database}/documents {
    // Cada usuario solo accede a su árbol bajo users/{uid}/...
    // Incluye storyTable/biblioteca y subcolecciones (characters, houses, etc.).
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
